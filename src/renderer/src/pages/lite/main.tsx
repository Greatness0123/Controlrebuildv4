import React from 'react';
import ReactDOM from 'react-dom/client'
import LitePage from './LitePage'
import '../../index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LitePage />
  </React.StrictMode>,
)
